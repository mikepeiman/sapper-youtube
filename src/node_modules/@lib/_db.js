import { init } from "@lib/mongo";

export async function get(req,res,next) {
    const { db } = await init();
    const comments = await db.collection("comments").find({}).toArray()
    console.log(`🚀 ~ file: db.js ~ line 7 ~ get ~ comments`, comments)
}


