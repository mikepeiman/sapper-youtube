// import mongo from 'mongodb'
console.log(`Mongo.js called`)
const mongo = require('mongodb')
let client = null
let db = null
export async function init() {
    if(!client) {
        client = await mongo.MongoClient.connect("mongodb://localhost")
        db = client.db("youtube-sapper")
    }
    console.log(`🚀 ~ file: mongo.js ~ line 9 ~ init ~ client`, client)
    console.log(`🚀 ~ file: mongo.js ~ line 10 ~ init ~ db`, db)

    return { client, db }
}